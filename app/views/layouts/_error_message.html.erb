<% if model.errors.any? %>
<div id="error_explanation">
  <h2><%= model.errors.count %> 件のエラーが発生したため、保存されませんでした。</h2>
  <ul>
    <% model.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>